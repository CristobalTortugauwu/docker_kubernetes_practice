Para este ejercicio, tuvimos que detener el contendor que se estaba ejecutando,
para luego simplemente borrar los tres contenedores.
        docker container stop <container_id>
Para borrar los contenedores tuvimos que ejecutar el siguiente comando:
        docker container rm <container>
Y finalmente borramos la imagen con el siguiente comando:
        docker image rm <image_id>
CONTAINER ID   IMAGE     COMMAND   CREATED   STATUS    PORTS     NAMES
REPOSITORY   TAG       IMAGE ID   CREATED   SIZE
